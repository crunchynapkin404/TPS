# TPS V1.4 Development Environment Configuration
# Copy this to .env for local development

# SECURITY: Generate a unique secret key for development
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-dev-only-change-this-for-production-7h8k9j0l1m2n3o4p5q6r7s8t9u0v1w2x3y4z

# Development settings
DEBUG=True
ENVIRONMENT=development
ALLOWED_HOSTS=localhost,127.0.0.1,[::1]

# CORS Configuration for development (allows all origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000

# Database Configuration (SQLite for development)
DATABASE_URL=sqlite:///db.sqlite3

# TPS Business Configuration
TPS_MAX_WAAKDIENST_WEEKS_PER_YEAR=8
TPS_MAX_INCIDENT_WEEKS_PER_YEAR=12
TPS_MIN_GAP_WAAKDIENST_DAYS=14
TPS_MIN_GAP_INCIDENT_DAYS=7
TPS_WAAKDIENST_HOURS_PER_WEEK=168
TPS_INCIDENT_HOURS_PER_WEEK=45

# Email Configuration (console backend for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Redis Configuration (optional for development)
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379

# Timezone
TIME_ZONE=Europe/Amsterdam

# Admin Configuration for development
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=admin123
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# Organization Configuration
ORG_NAME=TPS Development
ORG_DOMAIN=localhost
ORG_PHONE_PREFIX=+31 6
ORG_EMPLOYEE_ID_PREFIX=DEV

# Test Data Configuration
TEST_DOMAIN=test.localhost
TEST_PASSWORD=test123
CREATE_TEST_USERS=True
TEST_USER_COUNT=8